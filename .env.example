# Copy to CI repository variables (never commit real values).
# Public GitHub EUPL-1.2 mirror target, as a tokened HTTPS remote:
GITHUB_MIRROR_URL=https://x-access-token:REPLACE_TOKEN@github.com/protagx/zbrett.git
