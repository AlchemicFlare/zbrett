[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "brett-nntp"
version = "0.0.0"
description = "zBRETT read-only NNTP frontend (E7)"
readme = "README.md"
requires-python = ">=3.11"
license = "EUPL-1.2"
authors = [{ name = "protagx", email = "hello@protagx.com" }]
dependencies = [
    
]

[tool.hatch.build.targets.wheel]
packages = ["src/brett/nntp"]

[tool.uv.sources]
brett-core = { workspace = true }
brettd = { workspace = true }
